# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: '2.5'
metadata:
  name: fioriyoeman
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://103.207.171.202:8021/
          client: "800"
        credentials:
          username: env:MY_USER
          password: env:MY_PASSWORD
        app:
          name: ZZG_FIORI_YOE
          description: Deploy Gowri App to stfin
          package: $TMP
          transport: ""
